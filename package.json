{
  "name": "asd-miner-hierarchy",
  "version": "1.0.0",
  "description": "A TypeScript library for managing and querying miner hierarchies in the ASDMiner ecosystem.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build:js": "bun build ./src/index.ts --outdir ./dist",
    "build:types": "tsc --project tsconfig.json",
    "build": "bun run build:js && bun run build:types"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}